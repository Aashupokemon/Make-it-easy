# Make-it-easy
This is the python program which is integrated with docker which is an interface which will help to fill the skill gap in industry. 
first you have to download the files and save in same directory.
then run Dockerfile build with path of the directory to build an image of the application.
then use docker-compose up command to start the application.
